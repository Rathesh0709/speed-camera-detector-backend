# Database dependencies for Navigation App
# PostgreSQL + PostGIS with async SQLAlchemy

# Core database libraries
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
geoalchemy2>=0.14.0

# FastAPI (if using the example endpoints)
fastapi>=0.104.0
pydantic>=2.0.0
pydantic[email]>=2.0.0
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0

# Optional: For database migrations
# alembic>=1.12.0

# Optional: For connection pooling and monitoring
# psycopg2-binary>=2.9.0  # For sync operations if needed

# Database Configuration
# Copy this file to .env in the project root and update with your actual values

# PostgreSQL connection string
# Format: postgresql+asyncpg://username:password@host:port/database
DATABASE_URL=postgresql+asyncpg://postgres:YOUR_PASSWORD@localhost:5432/navigation_app

# Optional: Enable SQL query logging (set to true for debugging)
DB_ECHO=false
